import type { Setup } from '../interfaces/interfaces'

export const INITIAL_SETUP:Setup  = {
    drawTime: 60,
    wordToDraw: {
        name: "",
        category: "",
        id: ""
    },
    lang: "es"
}